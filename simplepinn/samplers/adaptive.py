from simplepinn.samplers.uniform import UniformSampler


class AdaptiveSampler(UniformSampler):
    """
    Placeholder adaptive sampler.

    Uses uniform sampling for now while keeping a stable public API for future
    residual-based refinement.
    """
